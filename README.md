# ME536-Project
 
`clothoidal_paths.py` generates composite clothoid curves

`optimized_plane_curves.py` optimizes plane turns to minimize time

`turning_plane_paths.py` generates flat plane flight paths given a function defining roll angles over time.
